#include <stdio.h>
int main(){
    float m1 = 87; // maths marks
    float m2 = 90; // physics marks
    float m3 = 93; // chemistry marks
    float m4 = 92; // english marks
    float m5 = 92; // physical education marks
    float p = (m1 + m2 + m3 + m4 + m5)/ 5;
    printf("The Percentage of Class 12 is : %f",p );
    return 0;
}