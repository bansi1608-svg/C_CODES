#include <stdio.h>
#include <math.h>

int main() {
    int a,q;
    // printf("Enter a number :");
    // scanf("%d",&q);

    // int root = sqrt(a);
    // printf("The square root is : %d",root);

    q = pow(3,3); // pow(a,b) means a raised to b
    printf("%d",q);
    
    return 0;
}